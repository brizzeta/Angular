import { FilterByAlphabetPipe } from './filter-by-alphabet.pipe';

describe('FilterByAlphabetPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByAlphabetPipe();
    expect(pipe).toBeTruthy();
  });
});
